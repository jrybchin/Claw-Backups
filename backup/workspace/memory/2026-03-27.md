# 2026-03-27

- User said the Claw folder in the workspace contains the core instructions built together yesterday and wants that logic treated as intact.
- Confirmed the docs still exist, but startup continuity files had not been fully restored; began a recovery pass.
- User pasted extensive chat history from 2026-03-26 documenting how the Claw system was designed, including:
  - Google Docs/Drive workflows using GOG
  - permission preference for private write actions without separate approval
  - broad memory/source-handling preferences
  - recovery-system docs, Codex mirror setup, and response-logic tuning
  - document role classification and sweep behavior
- User pointed me to a full Google Drive transcript export: `ChatExport_2026-03-27`.
- Export was found in Drive as a Google Doc and read locally after export; it contains the long-form transcript of yesterday’s Claw-building conversation.
- Confirmed and reinforced durable rules from that export:
  - finish full implementation chains before reporting back
  - treat sweeps as true propagation passes, not mere version checks
  - keep Claw Plan and dynamic logic highly current
  - use broad source retrieval for school/high-stakes/system tasks
  - keep indexes concise and human-facing
- Durable memory was reconstructed and expanded from the pasted history, the ChatExport transcript, and the existing Claw docs.
- User shared the main Google Drive Claw folder link and wants it treated as the instruction anchor.
- Mapped the current Drive Claw structure against the local `Claw/` tree.
- Main Drive-vs-local difference: Drive also contains top-level docs such as Claw Plan, Active Tasks List, Learning & Corrections System, Human System Explanation, Codex Mirror Setup, Open Key, and a Logs folder, while the local workspace keeps most structured docs under `Claw/logic/`, `Claw/systems/`, and `Claw/features/`.
- User explicitly wants continuity/memory stored somewhere durable so nothing important is lost on restart.
- Reinforced the rule that major continuity context must be written into workspace files promptly rather than left only in session context or Drive docs.
- User surfaced an OpenClaw memory-hardening approach: stronger pre-compaction memory flush prompt, session transcript indexing, and periodic manual memory review.
- Verified in local OpenClaw docs that `agents.defaults.compaction.memoryFlush` and `agents.defaults.memorySearch.experimental.sessionMemory` are real supported config surfaces.
- Wrote a Claw system doc capturing the fix direction and the recommended config shape for preventing restart/session-loss memory failures.
- GitHub backup automation was set up: sanitized backup script, daily 4:30 AM cron job, working repo auth, and successful end-to-end backup push verification.
- Next requested task from the user: continue using the Drive transcript as continuity source and keep folding durable decisions into memory/system docs.
